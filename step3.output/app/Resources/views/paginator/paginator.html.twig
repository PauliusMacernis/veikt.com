{#
  Source: http://dev.dbl-a.com/symfony-2-0/symfony2-and-twig-pagination/
  Updated by: Simon Schick <simonsimcity@gmail.com>
  Parameters:
    * currentFilters (array) : associative array that contains the current route-arguments
    * currentPage (int) : the current page you are in
    * paginationPath (string) : the route name to use for links
    * showAlwaysFirstAndLast (bool) : Always show first and last link (just disabled)
    * lastPage (int) : represents the total number of existing pages
#}
{% spaceless %}
    {% if paginator.maxPages > 1 %}

        {# the number of first and last pages to be displayed #}
        {% set extremePagesLimit = 3 %}

        {# the number of pages that are displayed around the active page #}
        {% set nearbyPagesLimit = 2 %}

        <ul class="pagination">
            {% if paginator.currentPage > 1 %}
                <li><a href="{{ path(paginator.paginationPath, paginator.currentFilters|merge({page: paginator.currentPage-1})) }}{{ paginator.qUrl }}">Previous</a></li>

                {% for i in range(1, extremePagesLimit) if ( i < paginator.currentPage - nearbyPagesLimit ) %}
                    <li><a href="{{ path(paginator.paginationPath, paginator.currentFilters|merge({page: i})) }}{{ paginator.qUrl }}">{{ i }}</a></li>
                {% endfor %}

                {% if extremePagesLimit + 1 < paginator.currentPage - nearbyPagesLimit %}
                    <li class="disabled"><a href="#">...</a></li>
                {% endif %}

                {% for i in range(paginator.currentPage-nearbyPagesLimit, paginator.currentPage-1) if ( i > 0 ) %}
                    <li><a href="{{ path(paginator.paginationPath, paginator.currentFilters|merge({page: i})) }}{{ paginator.qUrl }}">{{ i }}</a></li>
                {% endfor %}
            {% elseif paginator.showAlwaysFirstAndLast %}
                <li><span class="disabled">Previous</span></li>
            {% endif %}

            <li class="active"><a href="{{ path(paginator.paginationPath, paginator.currentFilters|merge({ page: paginator.currentPage })) }}{{ paginator.qUrl }}"
                >{{ paginator.currentPage }}</a></li>

            {% if paginator.currentPage < paginator.maxPages %}
                {% for i in range(paginator.currentPage+1, paginator.currentPage + nearbyPagesLimit) if ( i <= paginator.maxPages ) %}
                    <li><a href="{{ path(paginator.paginationPath, paginator.currentFilters|merge({page: i})) }}{{ paginator.qUrl }}">{{ i }}</a></li>
                {% endfor %}

                {% if  (paginator.maxPages - extremePagesLimit) > (paginator.currentPage + nearbyPagesLimit) %}
                    <li class="disabled"><a href="#">...</a></li>
                {% endif %}

                {% for i in range(paginator.maxPages - extremePagesLimit+1, paginator.maxPages) if ( i > paginator.currentPage + nearbyPagesLimit ) %}
                    <li><a href="{{ path(paginator.paginationPath, paginator.currentFilters|merge({page: i})) }}{{ paginator.qUrl }}">{{ i }}</a></li>
                {% endfor %}

                <li><a href="{{ path(paginator.paginationPath, paginator.currentFilters|merge({page: paginator.currentPage+1})) }}{{ paginator.qUrl }}">Next</a></li>
            {% elseif paginator.showAlwaysFirstAndLast %}
                <li><span class="disabled">Next</span></li>
            {% endif %}
        </ul>
    {% endif %}
{% endspaceless %}